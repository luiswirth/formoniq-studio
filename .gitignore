/target
/.direnv

flamegraph.svg
perf.data
perf.data.old

/.claude
/.cursor

# wasm build output (regenerated by the deploy workflow)
/dist

# Local co-development against a checkout of the engine: a `[patch]` block
# pointing the git dependencies at sibling paths, passed with `cargo --config`.
# Never committed, so the manifest's pinned revision stays the truth for
# everyone else and for CI.
/.cargo/local.toml
